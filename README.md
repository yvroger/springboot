# springboot is a mini project to check how much I know about Sprongboot
# The "rest-endpoint-web" would display the return of a rest endoint to a web browser
# The "two-datasource" branch highlights configuration of 2 database connections with their corresponding repository methods with 2 different controllers 
# Finally the "repository-unit-tests" would test the repositories and controllers class methods
